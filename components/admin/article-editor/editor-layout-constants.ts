/** Décalage vertical des overlays (hauteur sticky de la barre d’édition). */
export const EDITOR_BAR_TOP = "3.75rem";

/** Même valeurs que `Header` : `h-20` / `md:h-24` (header fixe au-dessus du contenu). */
export const SITE_HEADER_HEIGHT_MOBILE = "5rem";
export const SITE_HEADER_HEIGHT_MD = "6rem";
